prototype(Neos.MarketPlace:VersionPreview) >
prototype(Neos.MarketPlace:VersionPreview) < prototype(TYPO3.TypoScript:Tag) {
    tagName = 'section'
    attributes {
        class = 'block block--versions'
    }
    content = TYPO3.TypoScript:Collection {
        collection = ${q(documentNode).find('versions').find('[instanceof Neos.MarketPlace:Version]')}
        itemRenderer = Neos.MarketPlace:Version
        itemName = 'node'
    }
}
